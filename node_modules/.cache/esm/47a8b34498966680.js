let r;_989‍.x([["default",()=>VehicleController]]);_989‍.w("rethinkdb",[["default",["r"],function(v){r=v}]]);

               class VehicleController {
  constructor(){
    console.log('BOOONM');
  }

  // connectDb(){
  //   return r.connect({
  //     host: 'localhost',
  //     port: 8080,
  //     db: 'ctsdb'
  //   });
  // }

  async getVehicles() {
    // console.log('startt');
    // const c =  await this.connectDb();
    // console.log('mk:::', c);
    r.connect({
      host: 'localhost',
      port: 8080,
      db: 'ctsdb'
    }, (error, conn) => {
      if(error) throw error;
      _989‍.g.console.log('mk:', conn);
    });
    return [1,2,3];
  }
}